Django>=5.2,<5.3
djangorestframework>=3.15,<4.0
django-filter>=24.3
Pillow>=10.4
python-dotenv>=1.0
whitenoise>=6.7
gunicorn>=22.0
psycopg2-binary>=2.9

# Optional payment gateway SDKs - uncomment the ones you use and set the
# corresponding environment variables. The app degrades gracefully when the
# SDK is not installed (the gateway is simply not offered at checkout).
# stripe>=10.0
# razorpay>=2.0
